schema_version: 1
primary_agent: "{{ agent }}"
adapter_path: ".planning/adapters/{{ agent }}/README.md"
canonical_skills_path: ".planning/skills"
notes:
  - Client-specific commands are operator actions.
  - Project workflow and approvals remain agent-neutral.

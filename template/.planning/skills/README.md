# Canonical skills

Skills are thin routes to Planning Lite modes and workflows. Canonical behavior lives under `.planning/control/`, `.planning/modes/`, and conditionally loaded `.planning/disciplines/`.

Optional usage logging is governed by `SKILL_USAGE_LOGGING.md` and is disabled by default.

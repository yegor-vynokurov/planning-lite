# Project-specific agent instructions

This file belongs to the current project and is never replaced by Planning Lite updates.

Add only durable repository-specific rules that are not already captured in:

- `PROJECT_RULES.md`;
- `DEFINITION_OF_DONE.md`;
- the active change specification or plan.

Examples:

- required commands or environments;
- language and style conventions;
- directories that must not be modified;
- domain-specific safety constraints;
- generated files and their source of truth.

# Generic adapter

Natural-language instructions always work. Ask the agent to read `AGENTS.md`, select one mode, and follow one named workflow under `.planning/control/`. Do not load the full `.planning/` tree.

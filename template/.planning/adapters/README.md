# Agent adapters

Adapters translate client-specific invocation and operator controls. The neutral contract is `.planning/control/AGENT_ADAPTER_CONTRACT.md`; capabilities are summarized in `registry.yml`.

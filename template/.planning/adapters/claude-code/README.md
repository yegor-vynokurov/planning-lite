# Claude Code adapter

Materialize thin wrappers under `.claude/skills/` when needed. Each wrapper points to the canonical `.planning/skills/<name>/SKILL.md`. Keep `CLAUDE.md` as a bridge to `.planning/control/ROOT_ROUTER.md`.

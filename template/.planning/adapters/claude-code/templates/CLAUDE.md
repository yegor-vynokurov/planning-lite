@AGENTS.md

## Claude Code adapter

Canonical Planning Lite skills live under `.planning/skills/`. Project wrappers, when installed, live under `.claude/skills/` and use `/planning-*` invocation.

Client commands such as context compaction or clearing a conversation are operator actions. Follow `.planning/control/SESSION_CHECKPOINT.md` before using them.

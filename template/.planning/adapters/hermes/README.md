# Hermes adapter

Register `.planning/skills/` as an external skills directory when the Hermes installation supports it, or copy thin wrappers into the configured Hermes skills location. Preserve the neutral root router and do not translate client commands into core workflow rules.
